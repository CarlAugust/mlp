# def loss_function(predicted_values, true_values, length):
    # return mean_square_error(predicted_values, true_values, length)

# def mean_square_error(predicted_values, true_values, length):
    # predicted_values = np.array(predicted_values)
    # true_values = np.array(true_values)

    # squared_errors = np.square(true_values - predicted_values)
    # return np.mean(squared_errors)

# def root_mean_square_error(predicted_values, true_values, length):
    # return np.sqrt(predicted_values, true_values, length)

# def mean_absolute_error(predicted_values, true_values, length):
    # predicted_values = np.array(predicted_values)
    # true_values = np.array(true_values)

    # absolute_errors = np.absolute(true_values - predicted_values)
    # return np.mean(absolute_errors)